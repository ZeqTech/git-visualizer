import type { ExecutionResult } from "./types";

/**
 * Execute: git reset (placeholder)
 * Reset to previous commit - simplified for now
 */
export function executeReset(): ExecutionResult {
  return {
    success: false,
    message: "Reset command not yet implemented",
  };
}
